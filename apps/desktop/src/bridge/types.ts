// 宿主类型 —— 从 @litools/sdk 统一导出，保证类型同构
export type {
  AppSettings,
  BaseInfo,
  IndexStatus,
  LauncherItem,
  LauncherSection,
  MatchRange,
  PluginViewState,
  SearchResult,
  SurfaceMetadata,
} from '@litools/sdk';

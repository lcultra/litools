# litools

litools is a Rust/Tauri desktop productivity platform inspired by command palettes and plugin-driven local tools.

Current milestone:

- Rust workspace foundation
- Tauri desktop shell
- Built-in command search provider
- SQLite schema foundation
- Plugin manifest and permission architecture skeleton
